<%= form_with(model: statement) do |form| %>
  <% if statement.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(statement.errors.count, "error") %> prohibited this statement from being saved:</h2>

      <ul>
        <% statement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :disposable_income, style: "display: block" %>
    <%= form.text_field :disposable_income %>
  </div>

  <div>
    <%= form.label :ie_rating, style: "display: block" %>
    <%= form.text_field :ie_rating %>
  </div>

  <div>
    <%= form.label :customer_id, style: "display: block" %>
    <%= form.text_field :customer_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
